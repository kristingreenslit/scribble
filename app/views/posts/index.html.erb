<h2>Posts <%= link_to "(+)", new_post_path %></h2>

<ul>
  <% @posts.each do |post| %>
    <li>
      <%= link_to post.title, post %>
    </li>
  <% end %>
</ul>

<!-- <table>
    <thead>
        <th>Post Date</th>
        <th>Post Title</th>
    </thead>
    <tbody>
      <br>
<% @posts.each do |post| %>
    <tr>
        <td><%= post.created_at %></td>
        <td><%= post.title %></td>
    </tr>
<% end %>
    </tbody>
</table> -->
